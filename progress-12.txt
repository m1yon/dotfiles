# PRD #12 — EM+IFS session skill + ifs-session-writer subagent

## #18 — done — 2026-04-25T20:00:00Z — PENDING

Summary: Slice 6 — Phase 5 (Befriend) + Phase 6 (Fears) live. Replaced the slice-5 "post-Phase-4 stub middle" with full Phase 5 + Phase 6 procedural content, then added new "post-Phase-6 stub middle" (only Phase 7 still stubbed). Phase 5: light pulse on transition (Phase 4 → Phase 5 is a non-high-risk transition, light pulse only per PRD §16); Schwartz-orthodox relationship-building question verbatim *"What does it want you to know?"* + optional follow-up *"What would help it relax?"*; reflection-only stance (doctrinal line 3 reinforced — Claude never tells the user what the part wants; if user asks, deflect with *"That's for it to tell you."*); propose-and-ratify Phase 5 → Phase 6 transition (real scope change per doctrinal line 6, plain-prose offer with three branches: ask / stay-here / close). Phase 6: light pulse on transition; **simple time-awareness only** for short tier (slice #7 builds the full wrap clock — slice 6 deliberately scoped to short-tier upper-bound check at §6-2); fear question verbatim *"What does it fear would happen if it stopped doing its job?"*; **optional protector→exile capture** at §6-4 — when user's fear-answer reveals a protected-exile relationship, ask one verification line (*"You named '<exile descriptor>' just now. Should I log that as what this part is protecting?"*), trust the answer, queue `record_protects { part_ref, exile_ref }` on confirm. The exile_ref may be a description-only placeholder string OR a `[[<existing-title>]]` wikilink (best-effort skill-side glob check against existing Parts/). One follow-up at §6-5 if first answer was brief; no looping deeper — that's Phase 7 territory. New session-state blocks: `phase5_state { befriend_complete, transition_to_phase_6_ratified }` and `phase6_state { fears_surfaced, protector_relationship_captured }` — both default `false`/`false`, both surfaced as event-log entries (`befriend_complete { part_ref }` and `fears_surfaced { part_ref }`) rather than direct frontmatter fields. New `focus_part` shape additions (mirrored on every `re_targeted_parts[]` entry): `befriend_notes: [string, ...]` (verbatim user-language strings from Phase 5), `fears: [string, ...]` (verbatim from Phase 6), `protects_ref: string | null`. Subagent system prompt: input contract gains `phase5_state`, `phase6_state`, and the three new per-part fields; new `befriend_complete` and `fears_surfaced` event-log types added; **part page body population (slice 6)** section added with explicit reflection-only rules for `## Role` / `## Fears` / `## What it needs from Self` from `befriend_notes` + `fears`, dated `(YYYY-MM-DD.)` suffix convention, ONE Write per new part (compose full body before write — no Write-then-Edit), append-dated-bullets-not-replace for existing parts. `## Burdens` kept light (Phase 7 territory). `## Origin story` untouched in slice 6. New step 6 in part-page handling for applying `record_protects`. `## Parts encountered` body sub-section template extended: "What it shared" now folds verbatim `befriend_notes` content as the dominant voice; new "Fears surfaced" and "Protects" lines render only when `fears`/`protects_ref` non-empty/non-null. PROTOCOL.md gained §5 (full Phase 5 procedural content: §5-1 through §5-4) and §6 (full Phase 6 procedural content: §6-1 through §6-6); old §5stub renamed to §6stub with only Phase 7 stubbed. `task format` ran clean (no .nix files touched in this slice's changes). No automated tests per PRD's "Testing Decisions" section; validation is dogfooding through the demo path from issue #18 (run `/em-ifs-session` long tier, walk Phases 1–6 cleanly, verify session note's `## Parts encountered` body sub-sections reflect befriend + fears content, verify part page's `## Role` / `## Fears` / `## What it needs from Self` got dated bullets).

Files touched:
- dotfiles/agents/skills/em-ifs-session/SKILL.md (slice description bumped to slice 6; `focus_part` schema gains `befriend_notes` + `fears` + `protects_ref`; `re_targeted_parts[]` entry shape mirrors; new `phase5_state` + `phase6_state` blocks in session-state init; replaced post-Phase-4 stub middle with full Phase 5 + full Phase 6 + new post-Phase-6 stub; §4-postphase routes into Phase 5 instead of stub; bail-handling list mentions Phase 5 / Phase 6; subagent dispatch input contract gains the new fields and contract description; doctrinal line 1 / 2 / 6 reinforced in Phase 5 / Phase 6 procedure)
- dotfiles/agents/skills/ifs-shared/PROTOCOL.md (slice description bumped; §5 Phase 5 full content; §6 Phase 6 full content with §6-2 short-tier time-awareness check + §6-4 protector→exile capture; §5stub renamed to §6stub; Slice 7+ placeholder list trimmed to Phase 7 + cycle detection + polarization + wrap clock + renames)
- dotfiles/claude/agents/ifs-session-writer.md (input contract gains `phase5_state` + `phase6_state` + per-part `befriend_notes` / `fears` / `protects_ref`; event_log gains `befriend_complete` + `fears_surfaced`; populating-frontmatter rules updated to call out slice-6 part-page-body data; new "Part page body population (slice 6)" subsection with reflection-only rules for `## Role` / `## Fears` / `## What it needs from Self`; part-page handling step 6 added for `record_protects`; `## Parts encountered` body template extended for "Fears surfaced" / "Protects" lines + befriend_notes-dominant "What it shared" line)

Advice:
- The Phase 5 → Phase 6 transition propose-and-ratify is the only mid-session ratify in slice 6 (the transition into Phase 5 is just a light pulse — phase transitions per PRD §16 are not all "scope changes"). Slice 6 makes the call that Phase 4 → Phase 5 is a phase transition (light pulse) but Phase 5 → Phase 6 is a scope change (propose-and-ratify). Argument: Phase 5 stays in relationship-building register; Phase 6 shifts to fears, which is a different relational vector and the user's experience changes register too. The PRD doesn't explicitly call out which inter-phase moves are scope changes vs. just transitions; flag if dogfooding shows the Phase 5 → Phase 6 ratify feels heavy. Could be downgraded to a one-line check ("OK to ask the fears question?") if propose-and-ratify reads as too formal here.
- §6-2 tier check: implements **only** the short-tier upper-bound proximity check (within 5 min of upper). Medium and long tiers proceed silently — full wrap clock for those is slice #7. The current short-tier behavior is: emit one soft-wrap-style line, branch on user pick. If user picks "wrap," skip Phase 6 entirely and route to §6stub (treat Phase 6 as deferred, like a graceful intra-phase bail). This may feel stilted ("we just got to fears and you're asking me to skip them?") — alternative would be to land Phase 6 step 3 (the fear question) regardless and treat the soft-wrap as advisory only. Slice 6 takes the conservative route. Re-evaluate when slice #7 lands the proper wrap clock.
- The `protects_ref` semantics are intentionally loose. For slice 6 it can be ANY string: a description-only placeholder ("the small one inside") OR a wikilink form ("[[the small one at the window]]"). The skill does a best-effort glob check against `Parts/*.md` to upgrade descriptors to wikilinks, but doesn't error if the match misses — the descriptor stays as plain text in `protects:` frontmatter list, and the user can hand-edit later or wait for Phase 7 to formalize the relationship. The subagent applies the `record_protects` entry as-is; whatever the skill queued is what lands in YAML.
- Doctrinal line 1 (never voice parts) is the single-most-loaded discipline in Phase 5. The "what does it want you to know?" framing is exactly the moment a less-disciplined skill would slide into "let me tell you what I think it's saying" — slice 6 reinforces the rule three times in §5 (§5-3 deflection script, doctrinal line restatement at top of §5, "playing back the user's vivid phrase ONCE is the entire interpretive move available"). Watch for drift in dogfooding; if Claude starts paraphrasing parts even gently, tighten further.
- The subagent's "Part page body population (slice 6)" section is the most consequential schema-spreading change in this slice. Previously the subagent only wrote the empty-heading body template on `create_part`; now it actively populates `## Role` / `## Fears` / `## What it needs from Self` from Phase 5/6 data. The convention is **dated bullets** for existing parts (append, never replace) and **one composed Write** for new parts. If dogfooding shows the existing-part-append getting noisy over many sessions (10+ sessions per part), slice 7+ may want to introduce a "compress prior bullets into a synthesis paragraph" cleanup step — but that's a future concern.
- `## Burdens` is intentionally NOT populated in slice 6 even when fears strongly hint at a burden ("I'm not enough"). Burdens are Phase 7 territory (the "what is it carrying that isn't its" question) and burden-naming requires explicit user articulation. Inferring a burden from a fear shape would violate doctrinal line 2. Slice 6 leaves `## Burdens` as the empty-heading template line for new parts; if a future slice wants light burden-inference, document the rule explicitly and gate on the user having used burden-language ("I'm carrying," "it's a heaviness," "this isn't mine") in the session.
- Re-targeted parts (slice 5 stack) inherit the same Phase 5/6 treatment as the original focus IF Phase 5/6 ran on them. The current focus = `re_targeted_parts[-1]` (or `focus_part` if empty), and Phase 5/6 questions target the current focus. So in an A → B re-target case, B gets `befriend_notes` + `fears` populated; A's befriend_notes/fears stay empty (Phase 5/6 didn't run on A — it was re-targeted away from at Phase 4). The subagent's iteration handles this correctly because it checks `befriend_notes`/`fears` non-emptiness per-part before populating body sections — A goes through Phase-3-only path (no body section updates), B gets full population. Verified in the body-template logic.
- SKILL.md is now 699 lines (up from 558 in slice 5). Slice 5 advice flagged "SKILL.md crossing 700 = strongly consider hoisting Phase-2/3/4 procedural detail into PROTOCOL.md" — slice 6 lands at 699, right at the threshold. Phase 5 + Phase 6 added ~140 lines. The phase-headers structure is still readable and the redundancy with PROTOCOL.md is conscious (PROTOCOL.md is eager-loaded so the duplication is "in the same context" anyway). Slice #7 (Phase 7) is the natural moment to make the hoisting call — Phase 7 is going to add another ~150-200 lines minimum. If you do hoist, the move is: replace each Phase section in SKILL.md with a one-paragraph summary + `See ../ifs-shared/PROTOCOL.md §<N>`, and keep only doctrinal lines, session-state schema, pre-flight, check-in, naming, closing ritual, bail handling, role-play refusal, subagent dispatch contract in SKILL.md proper.
- Pre-existing `.gitignore` and `home/programs/claude-code.nix` drift from slice 1 left uncommitted yet again (matches slice 2/3/4/5 pattern). Same call as before — not in scope of issue #18.
- Issue #18 acceptance criterion "If a fear-answer reveals a clear protector→exile relationship, capture it via `record_protects { part_ref, exile_ref }` pending-change. Exile reference may be a description-only placeholder if the exile hasn't been contacted yet." — wired explicitly at §6-4 with the verification line + descriptor-or-wikilink semantics. The subagent applies the entry per OBSIDIAN.md rules (added to protector's `protects:` list, deduplicated, mirrored only via the protector side per the existing schema convention).
- Issue #18 acceptance criterion "Body sections on the part page populated by subagent: `## Role`, `## Fears`, `## What it needs from Self` get content from Phase 5/6 material" — wired in the new "Part page body population (slice 6)" section of the subagent prompt. Reflection-only rule explicit. Date-stamping convention `(YYYY-MM-DD.)` so the part page reads as a layered record.
- Issue #18 acceptance criterion "Reflection-only stance maintained" — restated in three places in slice 6 (§5-3 in SKILL.md, §5-3 in PROTOCOL.md, "Part page body population (slice 6)" sub-rule in subagent prompt). All three have the same shape: never paraphrase the part's voice; reflect the user's own phrasings; if the user asks for interpretation, deflect.
- Issue #18 acceptance criterion "Tier check at Phase 6: if tier is `short` and time is running out, soft-wrap is permitted" — wired at §6-2 as simple time-awareness. Medium / long tiers don't trigger anything in slice 6 — that's slice #7.
- Issue #18 acceptance criterion "No new pending-changes types beyond `record_protects`" — verified. `record_protects` has been in OBSIDIAN.md since slice 1; slice 6 is the first slice to actively queue it. Event-log gains `befriend_complete` + `fears_surfaced` (PRD §16 / event log types section in OBSIDIAN.md doesn't enumerate every event type — those are subagent-input types, defined alongside use). Add them to OBSIDIAN.md's event-log enumeration too, optionally — slice 6 didn't, but the event_log enumeration there has trailing `...` so it's not closed.
- The Phase 5 / Phase 6 flow assumes the user is willing to listen to the part from Self (capture and reflect what surfaces). If the user can't or won't engage in that interior listening — they freeze, can't get an answer, get distracted — the skill currently has no fallback. Phase 5 step 2 just waits, and the user's eventual non-substantive answer ("nothing's coming") doesn't advance `phase5_state.befriend_complete`. The propose-and-ratify Phase 5 → Phase 6 transition will then offer "stay here" as one branch, which reads stilted. If dogfooding shows this, slice #7 may want to add an explicit "if no substantive Phase 5 answer after one full prompt + 30s, gracefully skip to closing ritual" branch.
- The `record_protects` entry's `exile_ref` value flows literally into YAML. If the descriptor contains characters that need YAML escaping (colons, brackets, quotes), the subagent should quote-wrap the value. For descriptions like "the small one inside" this isn't an issue, but if a user says "the part that learned: don't show weakness" the colon would break the YAML. Defensive: subagent should YAML-quote any descriptor containing `:`, `[`, `]`, `{`, `}`, `,`, `&`, `*`, `#`, `?`, `|`, `-`, `<`, `>`, `=`, `!`, `%`, `@`, `\``. Not implemented in slice 6 — flag for slice #7 if it bites.

## #17 — done — 2026-04-25T19:27:14Z — d91c0c7

Summary: Slice 5 — Phase 4 (continuation check + hybrid drift handling: thank-and-ask-space → re-glimpse → ratified re-target) + pulse cadence + dissociation cue (already in slice 4) all live. Replaced "post-Phase-3 stub middle" with full Phase 4 procedural content, then added new "post-Phase-4 stub middle" with pre-close full continuation check (third high-risk transition per PRD §16). Phase 3 entry pulse upgraded from slice-4 conditional ("only if `re_glimpses > 0`") to **unconditional full continuation check** (light pulse + texture pulse + Self-like-parts spotting). Added `re_targeted_parts[]` and `phase4_state { unblending_events, re_targets, drift_detected_count, last_pulse_result }` to session-state init. Re-targets stack linearly (A → B → C → D fine — cycle detection deferred to issue #18). Each re-targeted part carries `re_targeted_from` + `re_target_note` for the subagent's `### [[<title>]]` body sub-section. Original focus's `state_at_end` set to "re-targeted away from at Phase 4 — wouldn't step back, re-glimpse didn't restore" when applicable. Closing ritual targets the **current focus** = `re_targeted_parts[-1] || focus_part`. Bail handling explicitly flags **every** part touched (focus_part + every re_targeted_parts entry that reached Phase 3 engagement) with `set_left_without_resolution`, per PRD §40. Doctrinal line 7 reinforced in SKILL.md AND PROTOCOL.md with explicit failure-mode rationale: drift handling NEVER recurses into a full embodied engagement on the blending part — one ~30s thank-and-ask-space, then re-glimpse, then ratified re-target. PROTOCOL.md gained §4 (full Phase 4 procedural content: §4-pulse, §4-detect, §4-handle, §4-stack, §4-postphase) and updated §5stub to post-Phase-4. Subagent input contract gained `re_targeted_parts[]` and `phase4_state`; populating-frontmatter rules updated for `unblending_events`, `re_targets`, multi-part `parts_touched` / `new_parts` / `permission_granted` lists; `## Parts encountered` body template now iterates focus_part + each re_targeted_parts entry; part-page handling section renamed to slice-5 with explicit `set_left_without_resolution` step. Event log types added/specified in usage: `light_touch_step_back { success: bool }`, `re_target { from, to }`, `pulse_check { result }` — all already in OBSIDIAN.md event-log list, now actively logged by Phase 4. `task format` ran clean. No automated tests per PRD.

Files touched:
- dotfiles/agents/skills/em-ifs-session/SKILL.md (slice description, doctrinal line 7 reinforcement, session-state init gains re_targeted_parts[] + phase4_state, Phase 3 entry pulse upgraded to unconditional full continuation check, replaced post-Phase-3 stub with full Phase 4 + new post-Phase-4 stub with pre-close full continuation check, closing ritual current-focus targeting, bail handling flags every-part-touched with set_left_without_resolution, subagent dispatch input contract gains re_targeted_parts[] + phase4_state)
- dotfiles/agents/skills/ifs-shared/PROTOCOL.md (slice description, doctrinal line 5 reinforced with failure-mode rationale, new §4 Phase 4 full content, §5stub renamed post-Phase-4, slice 6+ placeholder list trimmed)
- dotfiles/claude/agents/ifs-session-writer.md (input contract gains re_targeted_parts[] + phase4_state; populating-frontmatter rules updated for multi-part lists + unblending_events + re_targets; ## Parts encountered body template iterates focus_part + each re_targeted_parts entry with re_target_note; part-page handling section renamed to slice 5 with explicit set_left_without_resolution step; sub-rule on re-targets updated to reference re_target_note verbatim)

Advice:
- Phase 3 entry pulse changed from slice-4 *conditional* (only if `re_glimpses > 0` or §1d engagement happened) to slice-5 **unconditional** full continuation check. PRD §16 says full continuation check at three high-risk transitions; this matches. The trade-off: in clean Phase 1 cases, the user just glimpsed and the entry pulse can feel like friction. If dogfooding shows the pulse-at-Phase-3-entry interrupts a cleanly-landed glimpse, consider in slice 6+ scoping the pulse down to just the texture sub-question (skip "still here and oriented?" when texture is obviously settled). For now, unconditional matches the PRD literally.
- The pre-close full continuation check (§5stub post-Phase-4) is a third high-risk transition (PRD §16). Slice 5 implements it as **non-recursive** — drift detected at pre-close does NOT loop back into Phase 4 hybrid handling, it just notes the drift and proceeds to closing ritual. The closing ritual itself is the recovery move at this point. This is a design call not made explicit in the PRD; happy to be overridden in slice 6+ if dogfooding shows pre-close drift commonly warrants another round of thank-ask-space. The argument for non-recursion: at pre-close, the user is winding down — re-opening engagement here would violate the "wrap shortens the work, not the close" principle from SAFETY.md.
- `re_targeted_parts[-1]` (current focus) targeting in the closing ritual: the skill defaults to thank/permission-grant only the current focus. Earlier focuses get a one-line acknowledgement *if* re-targets happened, but no permission-grant by default. PRD §31 says propose-and-ratify on every scope change — granting permission for an earlier focus IS a scope change and would need its own propose-and-ratify. The skill's default is to skip that; user can override in plain prose at the closing-step-3 prompt ("OK to come back to X another time?" → "yes, and also to the original one too") and the subagent should pick up multiple `[[name]]` mentions in `permission_granted[]`. Verify by dogfooding the re-target case.
- The state_at_end overload: originally state_at_end was a one-line user-language state (e.g. "softer, less loud"); now it ALSO carries the "re-targeted away from at Phase 4 — …" sentinel for re-targeted-away-from focuses. This is intentional — it keeps the schema flat — but the subagent has to handle both shapes when synthesizing the body sub-section. Documented in the subagent prompt. If slice 6+ adds more state-at-end shapes (e.g. "proceeded to Phase 7 unburdening"), keep them as plain-prose one-liners following the same pattern.
- `phase4_state.drift_detected_count` is in the schema but not actually populated by the skill in slice 5 — the existing `pulse_check { result: "drift_detected" }` event-log entries are the source of truth. The field is reserved for slice 6+ if a quick dashboard query over drift counts becomes useful. Slice 5 leaves it at `0` — the subagent doesn't read it.
- Re-target stacking semantics: the *current* focus is always `re_targeted_parts[-1]` if non-empty, else `focus_part`. Pulse-checks and drift detection target the current focus. When B → C re-targets, B's `state_at_end` gets the re-target sentinel (overwriting any prior Phase-3-completion state), and B becomes a previous-focus entry in `re_targeted_parts[]`. The skill mutates B's state_at_end in place at re-target time. Subagent reads the final state. Verified by re-reading the §4-handle-3 routing branch + `state_at_end` field comments.
- Bail handling per PRD §40: "every part touched flagged with `left_without_resolution: true`." Slice 5 wires this for focus_part + every re_targeted_parts[] entry that reached Phase 3 engagement (`body_location` or `description` non-null). Self-like part engaged in §1d only (no Phase 3 focus, no part page) is excluded — the part exists only as a `phase1_state.self_like_part_detected: true` frontmatter signal. If dogfooding shows the §1d-Self-like-bailed-before-§1d-clean-routing case warrants its own treatment, add it in a later slice — currently it's invisible at the Parts/ level by design.
- Cycle detection (issue #18) is the natural successor — slice 5 deliberately allows `re_targets >= 3` and `same part blends at Phase 4 twice` without tripping anything. The PRD §19/§20 says cycle detection should fire on either signal. Slice 5 logs all the data needed (`re_target { from, to }` events + `light_touch_step_back { success }` events with the blending part identity in the surrounding transcript), so issue #18 just needs to pattern-match over `event_log`.
- The Phase 4 §4-handle-3 ratified-re-target propose-and-ratify offers three options ("pivot to it" / "come back to original next time" / "close here"). The PRD acceptance criterion shape matches. PRD §31 says "every scope change ratified" — this is one of the highest-stakes scope changes in the protocol (focus pivot mid-session) and gets the most explicit user choice. Trust the user's pick at face value (PRD doctrinal line). Silence-as-pick defaults to "close here" per the PRD.
- Demo path 4 from issue #17 (dissociation cue at locate-in-body) was already wired in slice 4's Phase 3 step 1; slice 5 doesn't change it. The dissociation-cued focus gets `set_left_without_resolution` per the bail-handling additions.
- SKILL.md is now 558 lines (up from 410 in slice 4). Per slice 4 advice the 100-line SKILL.md guideline broke at slice 2; structure is still readable per-phase. Phase 4 alone added ~150 lines. If slice 6 lands Phase 5 + Phase 6 (befriend + fears) at similar density, SKILL.md will cross 700 lines — at that point strongly consider hoisting Phase-2/3/4 procedural detail into PROTOCOL.md and having SKILL.md just reference. PROTOCOL.md is at 427 lines, plenty of room.
- Pre-existing `.gitignore` and `home/programs/claude-code.nix` drift from slice 1 *still* uncommitted (matches slice 2/3/4 pattern). Same call as before — leaving for whoever decides to address them in scope.
- `set_left_without_resolution` is the bail-side of `clear_left_without_resolution`. Both already in OBSIDIAN.md (slice 2 added the symmetric setter). Slice 5 is the first slice to *queue* `set_left_without_resolution` in the skill — bail handling now explicitly enumerates every-part-touched. Subagent's part-page handling step 5 applies them.

## #16 — done — 2026-04-25T19:30:00Z — 8b5895c

Summary: Slice 4 — Phase 2 (notice what's present + focus part selection with propose-and-ratify on divergence from trailhead) and Phase 3 (full embodied engagement: locate-with-dissociation-cue → describe → thank → request space → feel Self-energy in opened space → "how do you feel toward that part?") now live, plus naming step at end of Phase 3 (descriptive-phrase reflection-only, `Unnamed YYYY-MM-DD #N` deferred-naming fallback, existing-part collision check via "same as X, or new?"). Replaced "post-Phase-1 stub middle" with full slice-4 content; new placeholder is "post-Phase-3 stub middle" (Phases 4–7 still stubbed). Added `focus_part` and `trailhead_returned_to_open_threads` to session-state init and to subagent dispatch input contract; `focus_part` carries `working_title`, `surfaced_phrase`, `body_location`, `description`, `is_new`, `is_self_like`, `permission_granted`, `state_at_end`. Closing ritual step 1 now thanks the focus part by working title; step 3 asks permission to come back to it (sets `focus_part.permission_granted`). §1d-engage Self-like-part path now seeds `focus_part = { is_self_like: true, working_title: null }` and skips Phase 2 (the focus is already chosen via §1d propose-and-ratify) routing directly into Phase 3 — naming still happens at end of Phase 3. PROTOCOL.md gained §2, §3, §3-naming live sections; the "Slice 4+ content" placeholder collapsed to "Slice 5+ content" listing only Phase 4–7 + cycle/polarization/full-rename. The subagent system prompt got: §8 part-page schema (frontmatter + empty-headings body template + sub-rules), four-step part-page handling workflow (`create_part` / `update_last_seen` / collision check / best-effort existing-match-by-description), explicit population guidance for `parts_touched` / `new_parts` / `permission_granted` / `## Parts encountered` body sub-section, and updated input contract showing `phase1_state` and `focus_part` blocks. `task format` runs clean (no .nix files touched). No automated tests per PRD.

Files touched:
- dotfiles/agents/skills/em-ifs-session/SKILL.md (slice description, session-state additions, replaced post-Phase-1 stub with full Phase 2 + Phase 3 + naming + post-Phase-3 stub, closing ritual variants by engagement state, bail handling references Phase 2/3, subagent dispatch input contract gains focus_part + trailhead_returned_to_open_threads + populating-frontmatter rules)
- dotfiles/agents/skills/ifs-shared/PROTOCOL.md (§2 / §3 / §3-naming live sections; §5stub renamed to post-Phase-3; placeholder list trimmed to Slice 5+)
- dotfiles/claude/agents/ifs-session-writer.md (input contract gains phase1_state + focus_part + trailhead_returned_to_open_threads; populating-frontmatter rules for parts_touched / new_parts / permission_granted; expanded `## Parts encountered` and `## Open threads` body templates with population logic; new "Part page schema (§8)" section with full frontmatter + body template + sub-rules + slice-4 part-page handling workflow including collision check)

Advice:
- The §1d Self-like-part-as-focus path now skips Phase 2 (correct — the focus is already selected via the §1d propose-and-ratify) and routes straight to Phase 3. The skill seeds `focus_part.surfaced_phrase` from the user's phrasing of the Self-like part itself; the user's mid-§1d engagement *is* their Phase 2 surfacing. Naming still happens at end of Phase 3 (so the part may end up titled by what it says/feels then, not from §1d). If dogfooding shows the Self-like-as-focus path produces awkward Phase 3 framing (since the part already partially engaged in §1d's locate-thank-ask-space), consider in slice 5+ a "skip the locate/thank/ask-space sub-steps and pick up at §3-5 (feel Self-energy)" branch when `is_self_like: true`. As written in slice 4, the full Phase 3 runs from the top — that's acceptable and matches "engage as a part" doctrine.
- "Other parts that surface" handling (Phase 2 step 3 / SKILL.md) doesn't have a typed pending-changes entry — it's stored only in `transcript` and the subagent parses surfaced-but-not-engaged parts out for `## Open threads`. Acceptable for slice 4; if slice 5+ needs the surfaced-aside parts in frontmatter (e.g. for a richer dashboard query later), add a `note_surfaced_aside { phrase }` pending-changes type.
- The subagent's "best-effort existing-match-by-description" check (item 4 in part-page handling) Globs `Parts/*.md` and looks for exact-title or alias matches against `focus_part.surfaced_phrase`. Per PRD §33 the trust-the-user discipline still holds — the skill asks "same as X, or new?" inline, so by the time the subagent runs, `focus_part.is_new` reflects the user's answer. The best-effort check is a *defensive* second-pass that catches the case where the skill missed the existing-part check entirely (e.g. crisis exit, or rapid bail before naming). When it catches one, it skips the create and adds a `failed[]` entry — the user reconciles by hand.
- The subagent's "Existing-part collision check" (item 3): if `Parts/<title>.md` already exists when `is_new: true` was claimed by the skill, treat as `failed`. This is the safety net for the rare case where the skill thought a part was new but one was just created in a parallel/recent session. Doesn't roll back the session note. Recovery via `<date>-recovery.md`.
- Pulse-check at entry of Phase 3 is conditional in slice 4 (only if `re_glimpses > 0` or §1d engagement happened). PRD §16 says "full continuation check at three high-risk transitions: entering Phase 3, entering Phase 7, pre-close" — this is unconditional in the PRD. Slice 4 chose the conditional version because in clean Phase 1 cases the glimpse just landed and a pulse adds friction. Slice 5+ may want to revisit when Phase 4 (continuation check) lands; the unconditional version may be the right move once cycle detection enters the picture.
- `set_part_type` is wired through the subagent and the pending-changes log but the *skill* never queues it in slice 4. Reason: the user's Phase 2/3 framing rarely yields a clean manager/firefighter/exile classification, and PRD §30 says reflection-only on naming/classification — so `unknown` is the safe default. Slice 5+ Phase 5–6 (befriend/fears) is the natural surface for `set_part_type` queueing (e.g. if the user describes the part as "the one that scrambles in when I'm overwhelmed", that maps to firefighter). For now, all new parts land with `part_type: unknown` and the user can edit the part page by hand.
- `record_protects` similarly wired but unused in slice 4 — lands when Phase 7 confirms a protector relationship (next-next slice).
- The "trailhead returned to open threads" semantics: when the user picks the surfaced thing over the trailhead, the trailhead goes back to `## Open threads` of the *new* note (not back to `Trailheads.md`, since it was already converted to a `strike_trailhead` queue or was an open-threads pick). The skill drops the `strike_trailhead` queue if it was a Trailheads.md pick. Subagent reads `trailhead_returned_to_open_threads: true` and adds a `- [ ] <trailhead text> — surfaced but not picked as focus this session` line. Verify by dogfooding; if the wording reads stilted, the subagent's `## Open threads` template is the place to tweak.
- The §1d-Self-like-as-focus path in slice 4 sets `focus_part.is_self_like: true` and skips Phase 2 — but the skill currently doesn't queue `create_part` for the Self-like part until naming at end of Phase 3. So if the user bails between §1d-ratify-as-focus and end-of-Phase-3, no part page exists; bail handling correctly skips the `set_left_without_resolution` queue in that case (no part_ref to target). Frontmatter still records `self_like_part_detected: true`. Coherent.
- SKILL.md is at 410 lines now. PRD's "skill-building conventions" referenced "keep SKILL.md under 100 lines, references one level deep." The 100-line guideline has been broken since slice 2; the structure is still readable because each phase is a top-level section. If slice 5+ adds another 100+ lines (Phase 4 alone is non-trivial), strongly consider hoisting Phase-2/3 procedural detail out of SKILL.md into PROTOCOL.md and having SKILL.md just point — the PROTOCOL.md sections (§2, §3, §3-naming) already exist and could be the single source. Trade-off: duplication is convenient mid-session because eager-loaded PROTOCOL.md is right there; hoisting reduces drift but means the skill has to context-switch more.
- Pre-existing `.gitignore` and `home/programs/claude-code.nix` drift from slice 1 *still* uncommitted (matches slice 2/3 pattern). Same call as before — leave for whoever decides to address them in scope.
- Issue #16 acceptance criterion "If focus part already exists (user mentions matching description), subagent updates `last_seen` rather than creating a new page" — wired both at the skill level (existing-part check at naming time, asks "same as X, or new?") AND at the subagent level (best-effort exact-title/alias Glob match if `is_new: true`). Two-layer defense.

## #15 — done — 2026-04-25T19:06:36Z — 00f2ed3

Summary: Slice 3 — Phase 1 (Shift into Self) live. Replaced the slice-2 stub-middle in `dotfiles/agents/skills/em-ifs-session/SKILL.md` with full Phase 1 procedural content: §1a glimpse delivery (point-at-the-door, reads `default_glimpse:` from `IFS.md` frontmatter with the canonical default `"What is here when there is no problem to solve?"`, types prompt + brief pause-line then waits), §1b texture question verbatim (*"Where does it feel located, if anywhere — and does it feel already-here or achieved?"*), §1c Self-like-parts spotting gate (clean texture proceeds silently; suspect texture offers ONE imitator observation as a question — never asserts; user denies → trust the answer; user unsure → re-glimpse once with briefer form, increment `re_glimpses`, second-pass unsure treated as soft denial), §1d engage-Self-like-part constrained mini-loop (locate → thank → ask space ONLY — explicitly NOT the full Phase 3 protocol; if user deep-engages, redirect lightly; re-glimpse after; if texture clean → proceed with `self_like_part_detected: true` preserved but Phase 7 NOT blocked since the part stepped back; if still murky / part won't make space → propose-and-ratify "let it be the focus today, or close" — focus path sets `focus_part_is_self_like = true` and de facto blocks Phase 7 via the `self_like_part_detected: true` frontmatter signal). Added `phase1_state` to session-state init (`{ self_texture, self_like_part_detected, re_glimpses, focus_part_is_self_like }` with safe defaults). Added `phase1_state` to subagent dispatch input contract. Renamed stub middle to "post-Phase-1 stub middle." Updated bail handling and closing ritual step 1 to mention Self-like part contact in plain prose when one was engaged. Expanded `dotfiles/agents/skills/ifs-shared/PROTOCOL.md` with §1 (Phase 1 — full procedural content mirroring SKILL.md, plus all four imitator question shapes for §1c) and updated §6 (state load tier 3) to call out the imitator-load-trigger explicitly. Expanded `dotfiles/agents/skills/ifs-shared/FAQ.md` with the 4 imitators in long-form prose (each with What it is / How it appears / Why it's not Self / What to do); kept "Why 'no problem to solve?'", 11 i's, EM vs. Schwartz as short stubs. `task format` (the repo's lint equivalent — no `task lint` or `task test` defined) ran clean. No .nix files touched, so format was a no-op for this slice. No automated tests per PRD's "Testing Decisions" — validation is dogfooding (run `/em-ifs-session`, walk the three demo paths from issue #15, inspect resulting session-note frontmatter `self_texture` / `self_like_part_detected` / `re_glimpses`).

Files touched:
- dotfiles/agents/skills/em-ifs-session/SKILL.md (replaced stub middle with Phase 1 §1a–§1d; added phase1_state to session-state init and subagent dispatch contract; updated bail/closing ritual references)
- dotfiles/agents/skills/ifs-shared/PROTOCOL.md (added §1 Phase 1 full content + four imitator-question shapes; updated §6 tier-3 load trigger)
- dotfiles/agents/skills/ifs-shared/FAQ.md (added 4 imitators in long form; kept other topics as short stubs)

Advice:
- The `phase1_state` block is a parallel structure to `metadata` rather than nested inside it. Subagent (`dotfiles/claude/agents/ifs-session-writer.md`) does NOT yet know to read `phase1_state` and write its three fields (`self_texture`, `self_like_part_detected`, `re_glimpses`) into session-note frontmatter — the agent prompt's session note schema lists those fields with hardcoded defaults (`self_texture: clean | murky | unknown`, `self_like_part_detected: false`, `re_glimpses: 0`) but the dispatch input contract section doesn't mention `phase1_state`. Slice 4 should update the agent's input contract section + add an explicit "copy `phase1_state.*` into the session-note frontmatter" instruction. As-is, the skill emits the data but the agent prompt is silent on what to do with it; in practice an Opus-xhigh subagent will likely figure it out (the field names match), but tightening this is cheap and prevents a regression.
- The §1c "denies" path (user says "no, it really does feel already-here") sets `self_texture: clean` AND keeps `self_like_part_detected: false`. The §1d-clean-after-mini-loop path (user confirmed imitator → engaged → texture now clean) sets `self_texture: clean` but keeps `self_like_part_detected: true`. The distinction matters for Phase 7 gating in slice 4+: a confirmed-then-stepped-back Self-like part still surfaces in the frontmatter as detected (because it was), but does NOT trip the Phase-7 block (per PRD §11 — only the "won't make space → focus" path sets the block). The wording in §1d "Texture now clean" path is explicit; future slices reading the flag for Phase 7 should additionally check `focus_part_is_self_like` (the actual block trigger).
- Issue #15 acceptance criterion 7 says "session-note frontmatter populated: self_texture, self_like_part_detected, re_glimpses" — those three fields already exist in OBSIDIAN.md's session-note schema (slice 1) and in the subagent's session-note schema section (slice 2). Slice 3 wires the skill to *populate* them. Verified: SKILL.md state init defaults are `self_texture: "unknown"`, `self_like_part_detected: false`, `re_glimpses: 0`, `focus_part_is_self_like: false` — the first three map directly to schema fields; `focus_part_is_self_like` is internal-only (no schema field) and intentionally not surfaced (PRD §11 — `self_like_part_detected: true` is the de facto Phase-7 block signal until slice 4+ adds an explicit phase-7-blocked field if needed).
- The four imitator-question shapes in PROTOCOL.md §1c each carry a small bias (e.g., spiritual-bypass shape mentions "above the body"). These are reflection-only offers per doctrinal line 3, but the wording does pre-frame the user's answer slightly. If dogfooding shows the question-shape leading users toward "yes, that," neutralize the wording (e.g., drop the "above the body" hint and let the user describe location freely).
- `Trailheads.md` parser robustness flagged in slice 2 advice is still flagged — not addressed in slice 3 since trailhead parsing didn't change. Defer to slice 4+ if the open-threads / trailhead picks miss edge cases.
- The slice-3 stub-middle placeholder line "[Session middle skipped — Phases 2–7 land in later slices. Routing to closing ritual.]" still violates the "phases never narrated" doctrine (it's meta-about-the-build, not the protocol). Same status as slice 2: development-only artifact; disappears when Phase 2+ land.
- Pre-existing `.gitignore` and `home/programs/claude-code.nix` drift from slice 1 left uncommitted again (matches slice 2 pattern). The `.gitignore` change adds `progress-*.txt` to ignore — `progress-12.txt` is already tracked so this only affects new progress files.
- Issue #15 demo paths verifiable by reading: clean texture (path 1) → §1d-clean route, frontmatter `self_texture: clean`, `self_like_part_detected: false`, `re_glimpses: 0`. Suspect-then-denied / unsure-then-soft-denied (path 2) → frontmatter `self_texture: clean`, `re_glimpses: 1`. Suspect-confirmed-won't-make-space (path 3) → frontmatter `self_texture: murky`, `self_like_part_detected: true`, `re_glimpses: 1`, internal `focus_part_is_self_like: true`. All three paths route into §5stub then closing ritual.

## #14 — done — 2026-04-25T18:55:30Z — 73149e8

Summary: Slice 2 tracer skeleton. Rewrote `dotfiles/agents/skills/em-ifs-session/SKILL.md` from the slice-1 foundation stub into the full tracer-skeleton orchestrator: pre-flight refusals (unchanged), session-state initialization (start_ts / metadata / transcript / event_log / pending_changes), eager state load (last session note + Trailheads.md, both nullable), three-step check-in (mood + free-text safety gate; tier via single AskUserQuestion — the only one in the entire skill; trailhead = combined prose offer of last session's `## Open threads` unchecked items + Trailheads.md unstruck items, free-text pick, queues `strike_trailhead` if it maps to a Trailheads line), one-line echo, stub-middle placeholder ("[Session middle skipped — tracer skeleton...]"), full closing ritual (thank → ask-more → permission → rest-in-Self ~30–60s → step-out), bail handling (status: interrupted, ritual still runs), imminent-harm exit (status: crisis_exit, ritual SKIPPED, crisis link emitted FIRST then subagent dispatch), role-play refusal one-liner, single Agent dispatch with full input/output contract at session end. Created `dotfiles/claude/agents/ifs-session-writer.md` (Opus xhigh, tools: Read/Write/Edit/Glob, vault root pinned to `/home/michael/My Vault/`) with full subagent system prompt covering: input contract (metadata/transcript/event_log/pending_changes), output contract (written/failed/summary), session note schema (§9 — full frontmatter, all body sections including stub-friendly variants), pending-changes log application semantics (incl. typed entry validation, `part_ref` resolution post-rename, dedup on `append_alias`, idempotent `strike_trailhead`), write order (session note first; later failures populate `failed[]` + `<date>-recovery.md` stub without rolling back the note), crisis_exit special handling (full synthesis through trigger turn — `crisis_exit_marker` truncation held in reserve per PRD open question), doctrinal lines (no voicing parts, no synthesizing classifications). Updated `dotfiles/agents/skills/ifs-shared/OBSIDIAN.md` to add `set_left_without_resolution` alongside the existing `clear_left_without_resolution`. Updated `dotfiles/agents/skills/ifs-shared/PROTOCOL.md` to add §0 (check-in mechanics live), §5stub (stub middle), §5f (Phase 8 closing ritual live, full procedure), and renamed the placeholder to "Slice 3+ content" to reflect what's still pending. Removed `dotfiles/claude/agents/.gitkeep`. No automated tests per PRD; `task format` is a no-op since no .nix files were touched. Pre-existing `.gitignore` and `home/programs/claude-code.nix` drift from slice 1 left uncommitted (per slice 1 advice).

Files touched:
- dotfiles/agents/skills/em-ifs-session/SKILL.md (rewrote — slice-1 foundation → full tracer skeleton)
- dotfiles/agents/skills/ifs-shared/PROTOCOL.md (added §0 / §5stub / §5f live sections)
- dotfiles/agents/skills/ifs-shared/OBSIDIAN.md (added `set_left_without_resolution` to pending-changes schema)
- dotfiles/claude/agents/ifs-session-writer.md (new — full subagent system prompt)
- dotfiles/claude/agents/.gitkeep (deleted)

Advice:
- The PRD calls out that the subagent flags every part touched on bail with `left_without_resolution: true`. Existing OBSIDIAN.md only had `clear_left_without_resolution`; added the symmetric `set_left_without_resolution` setter. Both the subagent prompt and OBSIDIAN.md now reflect this. If a future slice's design notes use a different type name (e.g. `mark_left_without_resolution`), reconcile.
- Subagent `summary` line shape is left as plain log-style ("Logged: 2026-04-25, 35min, 2 parts touched.") with three concrete examples in the agent prompt. Refine to one canonical shape if dogfooding shows ambiguity. Open question per design doc.
- Subagent input contract uses `previous_session_link` (string `[[link]]` or `null`); skill resolves it via `Glob "Sessions/*.md"` and takes the most recent. Subagent has a defensive fallback if caller omits.
- "Phases never narrated" doctrine is hard to fully honor in the tracer-skeleton "stub middle" line — `[Session middle skipped — tracer skeleton. Phases 1–7 land in later slices. Routing to closing ritual.]` is meta about the build, not the protocol. Accept as a development-only artifact; it disappears in slice 3 when Phase 1 lands.
- The subagent's `<date>-recovery.md` stub is documented but not really exercised in slice 2 (only writes are session note + at most one Trailheads strike — partial failure unlikely). Slice 3+ will exercise more.
- Slice 3 needs to wire `## Open threads` parsing more carefully — current SKILL.md says "unchecked `- [ ]` items" without specifying parser robustness. Tolerant parser is fine; flag if dataview-style task aggregation is desired.
- Renamed slice-2 PROTOCOL.md placeholder to "Slice 3+ content" — lists Phases 1–7, cycle detection, polarization work, naming, aliases/renames as remaining. That's a lot for one slice; expect further sub-slicing.
- `~/.claude/agents/` symlink continues to resolve correctly through the nix store; verified post-commit.
- The `effort: xhigh` frontmatter field on the agent file follows the design doc's `model: opus, effort: xhigh` recipe. If Claude Code rejects the `effort:` key (not all harness versions support it), drop to `model: opus` only — the dispatch will still work, just at default effort.

## #13 — done — 2026-04-25T18:35:34Z — 429ffa8

Summary: Slice 1 foundation. Created `dotfiles/agents/skills/em-ifs-session/SKILL.md` (61 lines, conversational-orchestrator only, eager-loads `../ifs-shared/PROTOCOL.md`); created `dotfiles/agents/skills/ifs-shared/` reference dir with PROTOCOL.md (stub + phase spine + Self-like-parts terse pattern-match list), OBSIDIAN.md (vault root pinned, folder structure, IFS.md homepage Dataview template, part/session schemas, pending-changes log schema, event log types, write order), SAFETY.md (pre-flight refusal paths 1+2, mood-step refusal criteria, imminent-harm pattern match, dissociation cue, tier matrix with two-factor Phase 7 gate, wrap behavior), TAXONOMY.md (manager/firefighter/exile + reclassification note), FAQ.md (stub with planned topics). Added `dotfiles/claude/agents/.gitkeep`. Wired `.claude/agents` symlink in `home/programs/agents.nix` (added `".claude/agents".source = outOfStore "${dotfiles}/claude/agents";`). Removed pre-existing empty `~/.claude/agents` directory before rebuild so home-manager could create the symlink. `task rebuild-home` succeeded; `~/.claude/agents/` resolves through nix store back to `dotfiles/claude/agents/`. `task format` reflowed `home/programs/claude-code.nix` (incidental, not committed). No automated tests planned per PRD ("Testing Decisions" section); validation is dogfooding.

Files touched:
- dotfiles/agents/skills/em-ifs-session/SKILL.md (new)
- dotfiles/agents/skills/ifs-shared/PROTOCOL.md (new, stub)
- dotfiles/agents/skills/ifs-shared/OBSIDIAN.md (new)
- dotfiles/agents/skills/ifs-shared/SAFETY.md (new)
- dotfiles/agents/skills/ifs-shared/TAXONOMY.md (new)
- dotfiles/agents/skills/ifs-shared/FAQ.md (new, stub)
- dotfiles/claude/agents/.gitkeep (new)
- home/programs/agents.nix (added one symlink line)

Advice:
- `~/.claude/agents` is created by Claude Code itself as an empty directory at startup. Home-manager's `mkOutOfStoreSymlink` refuses to overwrite a non-managed directory at the link path, so a fresh rebuild on a new machine OR after Claude Code recreates that dir will fail with "existing file is in the way". Mitigation: `rmdir ~/.claude/agents` before `task rebuild-home`, OR have agents.nix emit a hook that removes it. Document in next slice's setup.
- `task` defines no `test` or `lint` target. The repo's lint equivalent is `task format` (nixfmt over all `*.nix`). Running it reflowed `home/programs/claude-code.nix` (multi-line `export` joined onto one line) — that's a pre-existing format drift, unrelated to this slice. Left it uncommitted.
- `.claude/settings.local.json` accumulates permission grants from interactive Claude runs (e.g. `Bash(rmdir ...)`). Don't stage those in feature commits — they are per-machine noise.
- The PRD says skill description is pattern-matched for invocation. Used the keyword set: `/em-ifs-session`, "IFS session", "parts work", "glimpse", "Self-led session". Adjust if invocation isn't reliable.
- Vault root `/home/michael/My Vault/` is hardcoded in `OBSIDIAN.md`. Per PRD §52 it must NOT be in `SKILL.md`. Per PRD it must also be in the `ifs-session-writer` subagent system prompt (slice 2+).
- Authoritative spec is `research/ifs-skill-design.md` — defer to it on any conflict with the PRD.
- Phase 7 two-factor gate is actually four conjunctive factors per PRD §4f (tier permits + protector permission + clean Phase 1 texture + no current Self-like-part). The PRD calls it "two-factor" but lists four. Captured all four in SAFETY.md tier matrix; flag if you read this and find a contradiction with the design doc.
- The skill's pre-flight refuses on missing `crisis_fallback:` frontmatter on `IFS.md` even if the file exists. Acceptance criterion 1 is more specific than just "missing IFS.md."
- `Trailheads.md` may not exist on first session; treat missing as empty list (documented in OBSIDIAN.md). Don't bootstrap-create it — only `Sessions/` and `Parts/` per PRD §46.
- The `ifs-session-writer` subagent (under `dotfiles/claude/agents/`) is NOT built in this slice. The slice 1 acceptance criteria only require the directory exists and is wired into Nix. Sub-issues for slice 2+ will populate it.
